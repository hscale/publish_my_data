<h1>Resources</h1>

<% @resources.each do |r| %>
  <%= link_to (r.label || r.uri), resource_path_from_uri(r.uri) %><br/>
<% end %>

<%= paginate @resources %>

<br/>

Get this list as:
<%= link_to 'N-triples', :format => 'nt', :_page => @page, :_per_page => @per_page %>
<%= link_to 'Turtle', :format => 'ttl', :_page => @page, :_per_page => @per_page %>
<%= link_to 'RDF/XML', :format => 'rdf',:_page => @page, :_per_page => @per_page %>
<%= link_to 'JSON', :format => 'json', :_page => @page, :_per_page => @per_page %>