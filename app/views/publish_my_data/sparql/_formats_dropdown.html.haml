= render "publish_my_data/sparql/formats_dropdowns/#{@sparql_query.query_type.to_s}"

:javascript
  $(function() {
    var formatSelect = $('.api-links select.formats');
    formatSelect.change(function() {
      var newVal = formatSelect.val();
      $('.api-links code').text(newVal);
    });
    formatSelect.change(); // trigger the change.
  });